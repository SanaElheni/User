<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#F9F3E7"
    android:gravity="center"
    android:orientation="vertical"
    android:padding="40dp">

    <!-- ID DOIT ÊTRE placeholder_title -->
    <TextView
        android:id="@+id/placeholder_title"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="🚧 Module en développement 🚧"
        android:textSize="20sp"
        android:textStyle="bold"
        android:textColor="#1F5C2E"
        android:gravity="center"
        android:layout_marginBottom="20dp" />

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Cette fonctionnalité sera bientôt disponible"
        android:textSize="16sp"
        android:textColor="#666666"
        android:gravity="center"
        android:layout_marginBottom="40dp" />

    <!-- ID DOIT ÊTRE btnBack -->
    <Button
        android:id="@+id/btnBack"
        android:layout_width="match_parent"
        android:layout_height="55dp"
        android:text="Retour au Dashboard"
        android:background="#1F5C2E"
        android:textColor="#FFFFFF"
        android:textSize="16sp" />

</LinearLayout>